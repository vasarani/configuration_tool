- name: install packages
  hosts: local
  connection: local
  become: yes
  tasks: 
   - name: install package
     ansible.builtin.package:
       msg: "{{ item }}"
     loop:
     - mysql
     - nginx
     - zip  